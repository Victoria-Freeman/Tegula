plugins {
    alias(libs.plugins.android.library)
}

def rootName = rootProject.name
def colorsPath = (rootName == 'Tegula') ? ':colors' : ':Tegula:colors'

android {
    namespace 'com.venddair.tegula.themes'
    compileSdk 36

    defaultConfig {
        minSdk 21
    }
}

dependencies {
    api project(colorsPath)
}
